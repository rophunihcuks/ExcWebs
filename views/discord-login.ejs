<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>ExHub — Sign In</title>
  <link rel="icon" type="image/png" href="/img/ExLogo2.png" />
  <link rel="stylesheet" href="/css/mainv2.css" />
</head>
<body class="page-dark">
<header class="site-header">
  <div class="container header-inner">
    <a href="/" class="logo">
      <span class="logo-icon">
        <img src="/img/ExLogo2.png" alt="ExHub Logo"
             style="width:100%;height:100%;object-fit:cover;border-radius:12px;" />
      </span>
      <span class="logo-text">ExHub</span>
    </a>

    <nav class="header-nav">
      <% if (user) { %>
        <!-- Sudah login: tampilkan user + Dashboard + Sign Out -->
        <div class="header-user">
          <div class="header-user-pill">
            <% if (user.avatar) { %>
              <img
                src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png?size=64"
                alt="Avatar"
                class="header-user-avatar"
              />
            <% } else { %>
              <span class="user-icon"></span>
            <% } %>
            <span class="user-name"><%= user.global_name || user.username %></span>
          </div>
          <a href="/dashboard" class="btn btn-subtle" style="margin:0 8px;">
            Dashboard
          </a>
          <form action="/logout" method="post" style="display:inline;">
            <button type="submit" class="btn btn-ghost">Sign Out</button>
          </form>
        </div>
      <% } else { %>
        <!-- Belum login: tombol kecil di header (CTA utama tetap di card) -->
        <a href="/auth/discord" class="btn btn-ghost">Sign In</a>
      <% } %>
    </nav>
  </div>
</header>

<main class="page-main">
  <div class="container auth-wrapper">
    <!-- Sisi kiri: copy / highlight -->
    <div class="auth-card auth-card--side">
      <p class="dash-eyebrow">Account Linking</p>
      <h1 class="auth-title" style="margin-bottom:8px;">
        Connect Discord to ExHub
      </h1>
      <p class="auth-subtitle" style="margin-bottom:16px;">
        Sync your Discord identity to unlock the full ExHub experience:
        free keys, license dashboard, and future admin features.
      </p>

      <div class="hero-chip" style="display:inline-flex;align-items:center;margin-bottom:10px;">
        <img src="/img/discordlogo.png"
             alt="Discord"
             style="width:20px;height:20px;margin-right:8px;border-radius:4px;object-fit:cover;" />
        <span>Secure Discord OAuth2 login</span>
      </div>

      <p class="dash-section-caption" style="margin:10px 0 4px;">
        We will request:
      </p>
      <ul class="dash-section-caption" style="padding-left:20px;margin-top:0;">
        <li>Username &amp; display name</li>
        <li>Avatar &amp; banner (if any)</li>
        <li>Email address</li>
        <li>List of servers you joined</li>
      </ul>

      <p class="auth-footnote" style="margin-top:16px;">
        ExHub only uses this information to identify your account and link
        licenses/keys. You can revoke access anytime from your Discord settings.
      </p>
    </div>

    <!-- Sisi kanan: kartu utama login / already signed-in -->
    <div class="auth-card">
      <div class="auth-icon">
        <!-- Gunakan logo Discord sebagai icon utama -->
        <span class="auth-avatar" style="overflow:hidden;display:flex;align-items:center;justify-content:center;">
          <img
            src="/img/discordlogo.png"
            alt="Discord Logo"
            style="width:56px;height:56px;object-fit:cover;border-radius:16px;"
          />
        </span>
      </div>

      <% if (user) { %>
        <!-- Sudah login: jangan minta OAuth lagi -->
        <h2 class="auth-title">You are signed in</h2>
        <p class="auth-subtitle">
          Your Discord account is already connected to ExHub.
          Open your dashboard to manage keys and account metadata.
        </p>

        <a href="/dashboard" class="btn btn-primary auth-button" style="margin-top:16px;">
          Go to Dashboard
        </a>

        <p class="auth-footnote" style="margin-top:16px;">
          Not you? Click <strong>Sign Out</strong> in the top-right corner
          and sign in again with a different Discord account.
        </p>
      <% } else { %>
        <!-- Belum login: form Sign In normal -->
        <h2 class="auth-title">Sign in with Discord</h2>
        <p class="auth-subtitle">
          Connect your Discord account to generate free keys and access
          your ExHub license dashboard.
        </p>

        <% if (error) { %>
          <div class="auth-error" style="margin-bottom:16px;">
            Something went wrong (code: <%= error %>). Please try again.
          </div>
        <% } %>

        <a href="/auth/discord" class="btn btn-discord auth-button">
          <span class="btn-discord-icon">
            <img
              src="/img/discordlogo.png"
              alt="Discord"
              style="width:20px;height:20px;object-fit:cover;border-radius:6px;"
            />
          </span>
          <span>Continue with Discord</span>
        </a>

        <p class="auth-footnote">
          After signing in, you can open your <strong>Dashboard</strong> and
          start using <strong>Get Free Key</strong> instantly.
        </p>
      <% } %>
    </div>
  </div>
</main>

<!-- Proteksi dasar: blokir klik kanan + beberapa shortcut DevTools -->
<script src="/js/protect-devtools.js"></script>
</body>
</html>
